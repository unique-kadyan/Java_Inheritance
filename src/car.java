public class car extends Vehicle {

    public car(int numbers, String model, int size, double speed, boolean isDriver, boolean isOpen, int seating) {
        super(numbers, model, size, speed, isDriver, isOpen, seating);
        // TODO Auto-generated constructor
        super.getDeveloper();
     
    }

}
